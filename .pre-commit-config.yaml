repos:
  - repo: https://github.com/sqlfluff/sqlfluff
    rev: main 
    hooks:
      - id: sqlfluff-lint
        name: sqlfluff-lint-oracle
        args: ["--config", ".sqlfluff"]
        files: .*\.sql$
